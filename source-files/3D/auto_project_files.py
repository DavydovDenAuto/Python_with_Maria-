# Пример того, как Python выполняет выражения "ternary":
# i = 5 if a > 7 else 0
# try:
#     y_correction = 20 if PLATFORM == 'ANDROID' else 0
#     tap(common_screen.close_icon[0], y_correction=y_correction)
#     tap(common_screen.got_it_button)
# except TimeoutException:
#     pass

# from hamcrest import assert_that, contains_string
# elif screen == 'Curriculum Manager':
# sleep(3)
# library_screen.gathering_content.with_(timeout=80).should(be.not_.visible)
# curriculum_manager_courses_screen.lectures.with_(timeout=5).should(be.visible)
# assert_that(curriculum_manager_courses_screen.courses_tab.should(be.visible).get_attribute('class'),
#             contains_string('Selected'))